apiVersion: v1
kind: Service
metadata:
  name: mlflow-postgres-service
  namespace: mlops
spec:
  type: ClusterIP
  selector:
    app: mlflow-postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
