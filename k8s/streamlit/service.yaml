apiVersion: v1
kind: Service
metadata:
  name: streamlit-service
  namespace: mlops
spec:
  type: ClusterIP
  selector:
    app: streamlit
  ports:
    - port: 8501
      targetPort: 8501
      protocol: TCP
